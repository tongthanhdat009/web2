import React from "react";
import { createRoot } from "react-dom/client";

// Tạo root container
const root = createRoot(document.getElementById("root"));

// Render nội dung React
root.render(<h1>Hello, world</h1>);
